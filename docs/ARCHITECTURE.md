<!-- Generated by /ps-init on 2026-03-05 -->
<!-- Last updated: 2026-03-05 -->

# 项目架构文档

本文档描述 openclaw-xiaoai 的应用程序架构和目录结构。

> **最后更新**: 2026-03-05

## 系统架构

```
OpenClaw Gateway
    ├── xiaoai Channel Plugin
    │   outbound.sendText() → POST :4400/notify
    ├── Webhook /hooks/xiaoai (接收 ASR 文本)
    ↕ HTTP
桥接服务 (本项目)
    ├── NotificationServer (:4400) ← 接收 Plugin 推送 → TTS 播报
    ├── Gateway → 贾维斯: POST /hooks/xiaoai
    │           → 小爱同学: LocalHandler → HA
    ├── ProtocolServer (WS :4399) ← 与小爱通信
    ├── KWS / TTS / Speaker
    └── HA Controller + Monitor
    ↕ WebSocket :4399
小爱音箱 Pro (LX06)
```

## 数据流

**路径 A — 语音问答（贾维斯）：**
```
小爱 ASR → Gateway → POST /hooks/xiaoai → OpenClaw Agent
→ Plugin sendText() → POST :4400/notify → TTS → 小爱播报
```

**路径 B — 本地智能家居（小爱同学）：**
```
小爱 ASR → Gateway → LocalHandler → HA REST API → TTS → 小爱播报
```

**路径 C — 主动通知（OpenClaw → 小爱）：**
```
OpenClaw Agent → Plugin sendText() → POST :4400/notify → TTS → 小爱播报
```

## 目录结构概览

```
openclaw-xiaoai/
├── config.ts                        # 用户配置（webhook/notification/HA/KWS/TTS）
├── openclaw.json                    # OpenClaw Gateway 配置参考
├── openclaw.service                 # systemd 服务文件
├── src/
│   ├── index.ts                     # 入口：加载配置，创建并启动 XiaoAiApp
│   ├── xiaoai-app.ts                # 主应用：编排所有组件生命周期
│   ├── gateway.ts                   # 路由网关：双唤醒词分发
│   ├── notification-server.ts       # HTTP :4400 接收 OpenClaw 推送
│   ├── local-handler.ts             # 本地指令解析（开灯/关灯/空调等）
│   ├── speaker.ts                   # 音箱硬件控制（播放/唤醒/静音）
│   ├── tts.ts                       # Edge TTS 合成 + PCM 流式播放
│   ├── kws.ts                       # sherpa-onnx 唤醒词检测
│   ├── ha-controller.ts             # Home Assistant REST API 封装
│   ├── ha-monitor.ts                # Home Assistant WebSocket 状态订阅
│   ├── open-xiaoai.ts               # Open-XiaoAI 协议封装（单例）
│   ├── types.ts                     # 所有类型定义
│   └── protocol/                    # WebSocket 协议底层
│       ├── server.ts                # WS 服务端（:4399）
│       ├── codec.ts                 # 消息编解码
│       └── types.ts                 # 协议消息类型
├── plugin/                          # OpenClaw Channel Plugin
│   ├── package.json                 # 插件包描述
│   └── index.ts                     # registerChannel 实现
├── tests/                           # 测试文件
├── docs/                            # 项目文档
│   ├── ARCHITECTURE.md              # 本文件
│   ├── DEVELOPMENT.md               # 开发指南
│   ├── TESTING.md                   # 测试规范
│   └── spec/                        # 计划和任务文件
└── models/                          # KWS 模型文件（不在 git 中）
```

## 各目录职责说明

| 目录 | 职责 | 放置规则 |
|------|------|---------|
| `src/` | 桥接服务核心代码 | 所有运行时 TypeScript 源码 |
| `src/protocol/` | WebSocket 协议底层 | 仅协议相关代码（编解码、WS 服务端、消息类型） |
| `plugin/` | OpenClaw Channel Plugin | 独立部署到 OpenClaw Gateway 的插件代码 |
| `tests/` | 测试文件 | 按被测模块组织的测试用例 |
| `docs/` | 项目文档 | 架构、开发、测试等文档 |
| `models/` | KWS 模型 | sherpa-onnx 模型文件（.onnx/.model），不提交到 git |

## 核心模块关系

```
XiaoAiApp (xiaoai-app.ts)
├── ProtocolServer (protocol/server.ts)  ← WS 连接管理
├── Gateway (gateway.ts)                  ← 路由分发
│   ├── LocalHandler (local-handler.ts)   ← 本地指令
│   └── postToWebhook()                   ← OpenClaw webhook
├── NotificationServer (notification-server.ts)  ← 接收推送
├── TTS (tts.ts)                          ← 语音合成
├── KWS (kws.ts)                          ← 唤醒词检测
├── Speaker (speaker.ts)                  ← 音箱控制
├── HAController (ha-controller.ts)       ← HA 控制
└── HAMonitor (ha-monitor.ts)             ← HA 状态监控
```

## 重要实现说明

- **路径别名**: `@/*` → `src/*`（tsconfig.json paths 配置）
- **模块导入**: ESM 格式，导入路径必须带 `.js` 后缀
- **单例模式**: `OpenXiaoAIProtocol`（open-xiaoai.ts）和 `OpenXiaoAISpeaker`（speaker.ts）为模块级单例
- **异步响应**: 贾维斯路径是异步的——Gateway.postToWebhook() 返回空 text，实际响应通过 NotificationServer 异步送达
- **Token 双对**: webhook.token（桥接→OpenClaw）和 notification.token（OpenClaw→桥接）需分别在 config.ts 和 openclaw.json 中对齐

## 维护此文档

当进行以下变更时，请更新本文档：

- 新增目录或文件（添加到相应结构部分）
- 删除目录或文件（从相应结构部分移除）
- 修改核心模块关系或数据流
- 重构代码组织结构
